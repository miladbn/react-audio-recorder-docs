---
title: react-audio-recorder-hook
description: A simple and lightweight React hook for recording audio in web applications.
template: splash
hero:
  tagline: A simple, zero-dependency React hook for recording audio in web applications
  image:
    file: ../../assets/houston.webp
  actions:
    - text: Get Started
      link: /getting-started/installation/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/miladbn/react-audio-recorder-hook
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Features

<CardGrid stagger>
	<Card title="Simple API" icon="setting">
		Easy-to-use interface with intuitive methods and properties
	</Card>
	<Card title="Status Tracking" icon="information">
		Real-time recording status tracking with built-in states
	</Card>
	<Card title="Error Handling" icon="warning">
		Built-in error handling with descriptive error messages
	</Card>
	<Card title="Zero Dependencies" icon="approve-check">
		Lightweight package with no external dependencies
	</Card>
	<Card title="TypeScript Support" icon="document">
		Full TypeScript support with type definitions
	</Card>
	<Card title="Browser Compatibility" icon="laptop">
		Works with all modern browsers supporting MediaRecorder API
	</Card>
</CardGrid>

## Quick Example

```jsx
import { useReactAudioRecorder } from 'react-audio-recorder-hook';

function AudioRecorder() {
  const {
    startRecording,
    stopRecording,
    audioResult,
    status,
    errorMessage
  } = useReactAudioRecorder();

  return (
    <div>
      <button onClick={startRecording}>Start Recording</button>
      <button onClick={stopRecording}>Stop Recording</button>
      
      {status === 'recording' && <p>Recording in progress...</p>}
      {errorMessage && <p>Error: {errorMessage}</p>}
      
      {audioResult && (
        <audio src={URL.createObjectURL(audioResult)} controls />
      )}
    </div>
  );
}
```
