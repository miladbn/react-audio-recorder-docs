---
title: "React Audio Recorder Hook"
description: "A powerful, TypeScript-based React hook for recording, managing, and processing audio in web applications"
sidebar_position: 0
---

import { redirect } from "next/navigation";
import { Callout } from "fumadocs-ui/components/callout";
import { Steps } from "fumadocs-ui/components/steps";
import { Tabs } from "fumadocs-ui/components/tabs";

{/* This component will handle redirection for direct navigation */}
export default function LandingPage() {
// We'll only redirect if the URL exactly matches the index page
if (typeof window !== 'undefined' && window.location.pathname === '/docs/react-audio-recorder-hook') {
redirect('/docs/react-audio-recorder-hook/01-getting-started');
}

// Otherwise, we render the landing page
return (

<>
<div className="flex flex-col items-center text-center px-4 py-10 mb-8 bg-gradient-to-b from-gray-50 to-white dark:from-gray-900 dark:to-gray-800 rounded-lg">
<h1 className="text-4xl sm:text-5xl font-bold tracking-tight">
React Audio Recorder Hook
</h1>
<p className="mt-4 text-lg text-gray-600 dark:text-gray-400 max-w-3xl">
Add professional audio recording to your React applications with minimal
setup
</p>
<div className="mt-8 flex flex-wrap gap-4 justify-center">
<a
            href="/docs/react-audio-recorder-hook/01-getting-started"
            className="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700"
          >
Get Started
</a>
<a
            href="https://www.npmjs.com/package/react-audio-recorder-hook"
            className="inline-flex items-center justify-center px-6 py-3 border border-gray-300 dark:border-gray-700 text-base font-medium rounded-md text-gray-700 dark:text-gray-200 bg-white dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700"
          >
NPM Package
</a>
</div>
</div>

      <Callout type="info">
        Version 1.0.3 is now available with TypeScript types included!
      </Callout>

      <h2>Why React Audio Recorder Hook?</h2>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-6 my-8">
        <div className="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
          <h3 className="text-lg font-medium">📦 Zero Dependencies</h3>
          <p className="mt-2 text-gray-600 dark:text-gray-400">
            Built entirely with React hooks and browser APIs - no external
            dependencies
          </p>
        </div>
        <div className="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
          <h3 className="text-lg font-medium">🔍 Type Safety</h3>
          <p className="mt-2 text-gray-600 dark:text-gray-400">
            Full TypeScript support with comprehensive interfaces for type safety
          </p>
        </div>
        <div className="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
          <h3 className="text-lg font-medium">🔄 Complete Control</h3>
          <p className="mt-2 text-gray-600 dark:text-gray-400">
            Start, stop, pause, resume, and cancel recordings with ease
          </p>
        </div>
        <div className="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
          <h3 className="text-lg font-medium">⚙️ Customizable</h3>
          <p className="mt-2 text-gray-600 dark:text-gray-400">
            Configure audio constraints, chunk intervals, and MIME types
          </p>
        </div>
      </div>

      <h2>Quick Install</h2>

      <Tabs items={["npm", "yarn", "pnpm"]}>
        <Tabs.Tab>
        ```bash
        npm install react-audio-recorder-hook
        ```
        </Tabs.Tab>
        <Tabs.Tab>
        ```bash
        yarn add react-audio-recorder-hook
        ```
        </Tabs.Tab>
        <Tabs.Tab>
        ```bash
        pnpm add react-audio-recorder-hook
        ```
        </Tabs.Tab>
      </Tabs>

      <h2>Basic Usage</h2>

      ```tsx
      import React from "react";
      import useAudioRecorder from "react-audio-recorder-hook";

      function AudioRecorder() {
        const {
          startRecording,
          stopRecording,
          isRecording,
          recordingDuration,
          playRecording,
        } = useAudioRecorder();

        const handlePlay = async () => {
          const audioUrl = await playRecording();
          if (audioUrl) {
            const audio = new Audio(audioUrl);
            audio.play();
          }
        };

        return (
          <div>
            <div>Status: {isRecording ? "Recording..." : "Ready"}</div>
            <div>Duration: {recordingDuration.toFixed(1)}s</div>

            {!isRecording ? (
              <button onClick={startRecording}>Start Recording</button>
            ) : (
              <button onClick={stopRecording}>Stop</button>
            )}

            <button onClick={handlePlay}>Play</button>
          </div>
        );
      }
      ```

      <h2>Get Started in Minutes</h2>

      <Steps>
        <Steps.Step>
          <h3>Install the package</h3>
          ```bash
          npm install react-audio-recorder-hook
          ```
        </Steps.Step>
        <Steps.Step>
          <h3>Import the hook</h3>
          ```tsx
          import useAudioRecorder from 'react-audio-recorder-hook';
          ```
        </Steps.Step>
        <Steps.Step>
          <h3>Use it in your component</h3>
          ```tsx
          function AudioRecorder() {
            const { startRecording, stopRecording } = useAudioRecorder();
            // ...
          }
          ```
        </Steps.Step>
      </Steps>

      <h2>Ready to Learn More?</h2>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mt-6">
        <a
          href="/docs/react-audio-recorder-hook/01-getting-started"
          className="p-4 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800"
        >
          <h3 className="font-medium">Getting Started →</h3>
          <p className="text-sm text-gray-600 dark:text-gray-400">
            Learn the basics and set up your first recorder
          </p>
        </a>
        <a
          href="/docs/react-audio-recorder-hook/02-api-reference"
          className="p-4 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800"
        >
          <h3 className="font-medium">API Reference →</h3>
          <p className="text-sm text-gray-600 dark:text-gray-400">
            Explore all available options and methods
          </p>
        </a>
        <a
          href="/docs/react-audio-recorder-hook/04-examples"
          className="p-4 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800"
        >
          <h3 className="font-medium">Examples →</h3>
          <p className="text-sm text-gray-600 dark:text-gray-400">
            See practical examples and use cases
          </p>
        </a>
      </div>
    </>

);
}
